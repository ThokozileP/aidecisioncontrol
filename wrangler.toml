# Cloudflare Pages configuration for the static Astro build.
# The project has no server-side logic (no Workers Functions), so this
# file only declares the build output directory for `wrangler pages`
# CLI parity with the dashboard's Git integration settings.
name = "aidecisioncontrol"
pages_build_output_dir = "dist"
compatibility_date = "2026-08-01"
