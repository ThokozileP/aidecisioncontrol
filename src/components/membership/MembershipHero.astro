---
---

<section class="section membership-hero">
  <div class="wrapper membership-hero__inner">
    <p class="eyebrow">Membership</p>
    <h1>Join the AI Decision Control Forum</h1>
    <p class="lede membership-hero__lede">
      For practitioners, researchers, policymakers and professionals working across AI
      governance, risk, compliance, safety and regulation.
    </p>
  </div>
</section>

<style>
  .membership-hero {
    background: var(--color-navy);
    color: var(--color-white);
    padding-bottom: 6.5rem;
  }

  .membership-hero__inner {
    max-width: 42rem;
  }

  .membership-hero h1 {
    color: var(--color-white);
    font-size: clamp(2rem, 1.4rem + 2.4vw, 3rem);
    margin-top: var(--space-1);
  }

  .membership-hero__lede {
    margin-top: var(--space-2);
    color: var(--color-steel);
    max-width: 58ch;
  }
</style>
