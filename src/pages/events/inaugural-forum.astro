---
// The inaugural Forum's content now lives at /events itself (see
// src/pages/events.astro) rather than this nested route — redirect so any
// existing links/bookmarks to this URL keep working.
export const prerender = false;
return Astro.redirect('/events');
---
