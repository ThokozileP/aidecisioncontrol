---
// Permanent redirect for the old /founding-circle URL — the page moved to
// /expert-circle as part of the "Founding" → non-founder terminology
// cleanup. Kept as a thin redirect (matching the pattern already used by
// src/pages/admin/index.astro) rather than deleted, so existing bookmarks
// and backlinks still resolve.
export const prerender = false;
return Astro.redirect('/expert-circle', 301);
---
